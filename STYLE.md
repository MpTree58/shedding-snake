# 美术/音频风格军规(2026-07-31 定稿)

基准:**Kenney Pixel Platformer** 全套素材(CC0,见 assets-preview/LICENSES.md)。
所有自制素材(蛇脸、蛇身、蜕皮块等)必须严格对齐以下规则,不允许"差不多就行"。

## 1. 网格与尺寸

- 逻辑像素网格:**18×18 px / 格**(与 Kenney 瓦片一致);
- 游戏内渲染放大 **2 倍 = 36px/格**(整数倍缩放,像素永远锐利;渲染层 TILE 常量从 32 改 36);
- 自制素材一律先画 18×18 原稿,禁止直接画大图再缩小。

## 2. 调色板

- **只允许从 Kenney 瓦片 PNG 上取色**(取色器采样),禁止自己发明色值;
- 已采样定稿(2026-07-31,程序化读取像素):草绿 `#36e377`、中绿 `#2eb082`、
  深绿影 `#345551`、**通用描边 `#434a5f`**、泥土 `#cb815e`、泥土深 `#9f5a52`、红 `#dd442c`;
- 每个面最多 2 个色阶 + 1 个高光色,不用渐变、不用抖动(dithering)、不开抗锯齿。

## 2.5 连贯性军规(2026-07-31 返工教训,最重要的一条)

Kenney 的素材是**拼接系统**,不是独立贴纸。乱用必然出现"一块一块被切开"的廉价感:

- **地形 16 态自动拼接**:描边只出现在地块的暴露面。瓦片映射(暴露面→瓦片号):
  单块=0 · 横条左/中/右=1/2/3 · 竖柱顶/中/底=20/120/140 ·
  大地块角与边:左上21/顶22/右上23/左121/内部122/右123/左下141/底142/右下143 ·
  横条帽 tbl=1, tbr=3, 底柱帽 blr=140(渲染层 `terrain-<mask>` 命名,代码见 GameScene)。
- **云必须三段拼**(153 左+154 中+155 右),禁止单段使用;
- **背景带**:地平线用剪影帧(8 山/9 树/10 丘/11 松)+ 16 号纯色填充铺到底;
- **蛇同理**:身体分段贴图按连接方向生成,描边只画在非连接面(管道式),
  蛇头四方向各一张(脸永远正立,舌头朝行进方向),禁止旋转贴图;
- 新素材上线前先做"连贯性检查":平铺三块看接缝。

## 3. 造型语言(Kenney 风格拆解)

- **1px 描边**,颜色用填充色的深一档(不是纯黑);
- 圆角:外轮廓四角各切 1px(Kenney 标志性的软方块感);
- 表情:参考包内 Characters 瓦片的比例——眼睛 2×2px 起步、间距 1px、高光 1px;
- 蛇脸保持我们的 IP 设定(大眼珠 + 吐舌 + 死亡翻白眼),但用上述比例和取色重画。

## 4. 字体(Kenney Fonts,CC0)

| 用途 | 字体 |
|---|---|
| HUD 正文/提示 | **Kenney Pixel** |
| 小号数字(步数/长度) | **Kenney Mini** |
| 标题/LOGO | **Kenney Blocks** |

- 网页端 @font-face 加载,Phaser 里 fontFamily 直接引用;
- 场景启动前等 `document.fonts.ready`,避免闪回退字体。

## 5. 音频

- 格式:**MP3**(全浏览器通吃;源文件 OGG/WAV 不进构建);
- 映射(已选定候选,试听确认后锁死):
  吃苹果=pluck_001 · 移动=tick_001 · 断尾=drop_002 · 死亡=error_004 ·
  过关=confirmation_001 · 撞墙=bong_001 · 悔棋=back_001 · 开门=open_001;
- BGM 从 Juhani Junkala 5 首中选 1 首循环,音量压到 SFX 的 ~40%,M 键静音;
- 转码统一走 `node_modules/ffmpeg-static/ffmpeg -b:a 128k`(BGM 192k)。

## 6. 文件纪律

- `assets-preview/` 只是评估区,**永不进构建**;
- 选中的素材复制到 `public/assets/{tiles,audio,fonts}/` 再引用;
- 每新增一个第三方素材,**同一次提交里**更新 assets-preview/LICENSES.md(来源 URL + 许可证);
- 禁止使用任何商业游戏的素材(扒图/扒音效),无论多像"免费素材"。
